# Plantilla de variables de entorno.
# Copia este archivo como ".env.local" y completa los valores reales.
# El archivo .env.local NO se sube al repositorio (ver .gitignore).

# Usuario administrador del sitio
TT_ADMIN_USER=admin

# Contraseña del administrador
TT_ADMIN_PASS=tu-contraseña-aqui

# Clave secreta para firmar la cookie de sesión.
# Genera una cadena aleatoria larga, por ejemplo con:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
TT_AUTH_SECRET=cadena-aleatoria-larga-y-secreta

# --- Correo del formulario de contacto (Resend) ---
# Clave de API de Resend (https://resend.com → API Keys). Obligatoria para enviar.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
# Destino de los mensajes del formulario (por defecto sistemas@atghub.co)
CONTACT_TO=sistemas@atghub.co
# Remitente. Para producción usa un dominio verificado en Resend,
# p. ej. "Tuterritorio <contacto@tudominio.com>". Por defecto usa el de pruebas.
CONTACT_FROM=Tuterritorio <onboarding@resend.dev>
